name: Export BBL Auto Update APK 2.6.3
on:
  push:
    paths:
      - '.github/workflows/export-bbl-263.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - name: Download APK from Render
        run: |
          curl -fL --retry 10 --retry-delay 3 "https://bbl-launcher-build-v256b.onrender.com/BBL_BOXTV_2.5.6_ATUALIZAR_CLEAN_UNSIGNED.apk" -o "BBL_BOXTV_2.6.3_AUTO_UPDATE_SINGLE_DEVICE.apk"
          ls -lh "BBL_BOXTV_2.6.3_AUTO_UPDATE_SINGLE_DEVICE.apk"
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: BBL_BOXTV_2.6.3_AUTO_UPDATE_SINGLE_DEVICE
          path: BBL_BOXTV_2.6.3_AUTO_UPDATE_SINGLE_DEVICE.apk
          if-no-files-found: error
